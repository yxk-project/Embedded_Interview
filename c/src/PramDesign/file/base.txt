0、文件缓冲区
   输入输出文件缓冲区。
   全缓冲、行缓冲、无缓冲

1、文件指针
  存储文件信息，用结构体存储出。

3、打开文件
  fopen（文件名，使用文件方式） 返回指针

4、关闭文件
  fclose（文件指针）  成功返回0；否则返回EOF（-1）
  **在写入文件时，先向缓冲区写入数据，当数据充满缓冲区写入文件。但若缓冲区未满程序停止则数据丢失，
  因此操作玩文件后需要关闭文件。
5、读写字符
  
6、读写字符串

7、二进制文件读写

