1、结构体：存储不同类型数据；
2、定义：struct Student{};
3、结构体对齐操作。在内存当中，结构体默认按照非构造类型最大字节对齐，但连续字节之和小于等于
   最大字节，则拼接对齐。
   c语言数据类型：基本类型：
                构造类型：数组（数据数组、字符数组）、结构体类型、共用体类型、链表

                枚举类型：
                指针类型：
                空类型：
4、