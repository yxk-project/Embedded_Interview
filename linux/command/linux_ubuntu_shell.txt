第一章

一、启动
  1、linux启动过程（5步）
   内核引导：电源->bios自检->操作系统->/boot吻技安
   运行init：起始进程，首先读取/etc/inittab，运行启动进程。分为7个运行级别
   系统初始化： 文件（）.作用：激活交换分区、检查磁盘、加载硬件模块
   建立终端：
   用户登录：
二、系统目录结构
/bin 二进制文件  存放着命令
/boot 启动文件
/etc 系统管理配置文件
/dev
/home
/lib 动态链接共享库
/media 
/mnt 临时挂载文件
/opt 安装文件
/proc 虚拟文件，是内存映射
/root 系统管理员
/temp
/usr 共享资源
三、文件基本属性
 文件的两种属性：
   所属用户与组：chown
   用户权限（读写执行）chmod
   文件权限识别 d-是否为目录 rwx 读写可执行 --文件  l-链接文件 
四、文件管理命令
   ls -a -d -l
   cd
   pwd 
   mkdir -m -p
   cp
   rm
   mv
五、磁盘管理
df 
du
fdisk
六、软件管理
yum
apt
七、shell
 1、变量
   命名规则与c一直，=两边不能有空格，引用$变量名，字符串可以按照下标索引字符
 2、shell脚本传参
   $0……$9,0代表执行文件，1代表第一个参数。这里参数数量和c不一样，c要算上执行文件。${10}上了两位数要加{}
 3、数组
  创建：a=（v1 v2……）
  读取：a[]
  调用 $a[]

  关联数组（字典）
  创建：declare-A array=（[key]=value,[key]=value]
       declare-A array   array[key]=value
  调用：${array[key]}

  运算符：
    算术运算符：expr $a+$b
    关系运算符：
    布尔运算符：
    逻辑运算符：
    检测文件运算符：
  
  echo命令与printf

  test命令

  if 
  then
  ……
  else
  ……
   fi语句

for i in v1 v2……
do
……
done

while（条件）
do
……
done

case 值 in
模式）
……
；；
模式）
……
；；

八、函数
 定义：函数（）{}
 调用： 函数
 传参： 函数 参数1 参数2……   **这里参数用$0……等接受与执行文件传参一致

九、输出重定向（将输出传入到指定文件保存）

十、文件包含（调用其他文件）
  . 文件 **中间有空格
  

